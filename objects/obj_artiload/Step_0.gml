if input_check("shoot"){
	obj_artiload.focused = true};
	if focused = true{
	room_goto(rm_game)};
	//why am i faking loading?
	//well you see, we need the player to focus the mouse. that's literally it lmao - dt